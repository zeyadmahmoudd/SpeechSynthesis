function pulse_train = PulseTrain(f0, duration, fs)
    t = 0:1/fs:duration-1/fs;
    % Calculate period in samples
    T = round(fs/f0);
    % Create zero array
    pulse_train = zeros(1, length(t));
    % Set pulses at periodic intervals
    pulse_train(1:T:end) = 1;
    t = 0:1/Fs_male:duration-1/Fs_male;
    figure;
    plot(t, PulseTrain_male);
    title("Pulse train for male vowel")
    xlabel("time
end